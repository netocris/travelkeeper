# elasticsearch
elasticsearch:
  clustername: travelkeeper-cluster
  host: localhost
  port: 9300

# pagination
pagination:
  pagesize: 10

# log
logging:
  level:
    ROOT: INFO
    org.springframework: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"